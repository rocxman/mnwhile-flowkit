import type { LucideProps } from 'lucide-react';
import { LayoutPanelLeft } from 'lucide-react';

export function AssetsIcon(props: LucideProps): React.ReactElement {
  return <LayoutPanelLeft {...props} />;
}
