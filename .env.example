# =============================================================================
# OpenFlowKit — Environment Variables
# =============================================================================
# Copy this file to `.env` and fill in the values you need.
# All variables are OPTIONAL — users can also paste API keys directly
# in Settings → Brand → Flowpilot.
# =============================================================================

# --- AI (Gemini server-side fallback) ---
# Only needed if you want a server-injected default key.
# Users provide their own keys via the UI (BYOK).
GEMINI_API_KEY=

# --- Analytics (PostHog) ---
# Leave empty to disable analytics entirely.
# If you fork this repo, analytics will NOT fire until you add your own key.
VITE_POSTHOG_KEY=
